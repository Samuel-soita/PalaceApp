describe('Sample E2E Test', () => {
  it('should load the homepage', () => {
    cy.visit('/');
    // Add more assertions here as the app grows
    // cy.contains('Welcome').should('be.visible');
  });
});
